# Netlify SPA fallback — only relevant if you deploy to Netlify instead of
# Vercel (Netlify checks for a real file like sitemap.xml/robots.txt first,
# and only falls back to index.html for client-side routes).
/*    /index.html   200
